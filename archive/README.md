# Archive Directory

This directory contains archived files from the main project that are no longer actively used but preserved for reference.

## Structure

- `deploy/` - Archived deployment scripts from earlier iterations
- `scripts/legacy/` - One-off scripts used for debugging, fixes, and migrations
- `deployments/` - Historical deployment artifacts and records

## Note

These files were archived on 2024-03-19 during the wallet-balance-fix branch cleanup. They are kept for reference but should not be used in production. 